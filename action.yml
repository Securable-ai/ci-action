name: "Codex Security CI Scan"
description: "Tar a folder and upload to Codex Security CI endpoint"
inputs:
  server_url:
    required: true
    default: "https://api.codexsecurity.io/ci-code"
  api_key:
    required: true
  folder:
    default: "."
runs:
  using: "node20"
  main: "dist/index.js"
